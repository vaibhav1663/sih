version: "3"
services:
  client:
    build:
      context: ./client
    ports:
      - "3000:3000"
  server:
    build:
      context: ./server
    ports:
      - "5000:5000"
    depends_on:
      - mongo
  mongo:
    image: mongo
    ports:
      - "27017:27017"
